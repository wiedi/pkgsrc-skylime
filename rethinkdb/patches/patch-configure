$NetBSD$

--- configure.orig	2014-01-16 08:49:11.000000000 +0000
+++ configure
@@ -64,6 +64,8 @@ configure () {
             var PTHREAD_LIBS "" ;;
         Linux)
             var PTHREAD_LIBS -pthread ;;
+        SunOS)
+            var PTHREAD_LIBS "" ;;
         *) error "unsupported operating system: $OS" ;;
     esac
     require "Without tcmalloc"
